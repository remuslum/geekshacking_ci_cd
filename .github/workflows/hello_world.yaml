name: Hello_world

# trigger
on:
    - push

# one or more jobs
jobs:
    greetings:
        runs-on: ubuntu-latest
        steps:
        - id: s00
          # running some shell/bash command
          run: echo "current time in GitHub is $(date)"
        
        - id: s01
          run: cat ./README.md
